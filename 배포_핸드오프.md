# PRIZM 콘텐츠 스튜디오 — 배포·운영 핸드오프

> 다중 사용자 배포 상태와 트러블슈팅 기록. jayce 등 새 사용자가 실행하다 이슈가 생기면 이 문서부터 확인.
> 프로그램 기능·구조 상세는 `studio/작업내역_HANDOFF.md` 참고.
> 최종 갱신: 2026-08-25

---

## 1. 현재 상태 요약

- 로컬 단일 프로그램 → **"여러 명이 각자 PC 실행 + 모범 코퍼스 git 공유"** 배포 형태로 전환 완료.
- 품질 균일 장치: 동일 코드(규칙·가이드) + 모델 Opus 고정 + 생성 직전 자동 `git pull`(공유 코퍼스) + 큐레이션(담당자만 모범 등록).
- **아직 남은 것**: 각 사용자 GitHub 초대 수락 + 각 PC 개인설정(NAS·claude login). 초대·설정 전에는 다른 사람이 실행 불가.

---

## 2. 저장소 (둘 다 Mark-ji-rxc 개인계정·Private)

| 용도 | 저장소 | 비고 |
|---|---|---|
| **코드** | `github.com/Mark-ji-rxc/prizm-curation-contents` | 프로그램 본체. 비밀/개인파일은 `.gitignore`로 제외 |
| **모범 코퍼스** | `github.com/Mark-ji-rxc/prizm-curation-editor` | 담당자 목록·모범 콘텐츠. 생성 시 자동 pull |

- 원래 회사 org(`rxcompany/...`)에 두려 했으나 **접근 권한 문제**로 개인계정으로 이전함(코드·코퍼스 모두). 기존 rxcompany 저장소는 폐기 예정.
- `.gitignore` 제외 대상(커밋 금지): `**/nas.config.json`(NAS 비번), `**/ai.config.json`(apiKey), `studio/studio.config.json`, `saved-contents.json`·`saved-insights.json`·`저장콘텐츠_학습분석.md`·`state.json`·`.backups/`·`jobs/`·`uploads/`, `**/exports/`(NAS 이미지), `**/*.app/`, `검색결과/`·`prizm_all_*.json`, `prizm-curation-editor/`.

---

## 3. 담당자(큐레이터) — 모범 콘텐츠 등록 권한

- 파일: `prizm-curation-editor/curators.json` 의 `curators` 배열(각 항목 = 그 사람 PC의 `git config user.email`).
- 현재 담당자: `wlgusgh85@gmail.com`(지현호), `jayce@rxc.co.kr`(jayce).
- **담당자 추가/삭제**: curators.json 편집 → 커밋 → push 하면 각 PC에 자동 반영(코드 재배포 불필요).
- 담당자가 아니면 생성엔 코퍼스가 참고되지만 **모범 등록은 불가(읽기 전용)**.

---

## 4. 새 사용자 온보딩 순서 (jayce 기준)

0. **GitHub 초대 수락** — 저장소 2곳(contents·editor) 초대 메일 수락. (미수락 시 clone 전부 실패)
1. **Node.js** LTS 설치 (nodejs.org)
2. **Claude Code** 설치 + 본인 구독 계정 `claude login`
3. **git 이메일 설정**(담당자면 필수, curators.json 값과 일치):
   ```bash
   git config --global user.email "jayce@rxc.co.kr"
   git config --global user.name "Jayce"
   ```
4. **코드 clone**:
   ```bash
   git clone https://github.com/Mark-ji-rxc/prizm-curation-contents.git
   cd prizm-curation-contents
   ```
5. **`setup.command` 더블클릭** → 코퍼스 자동 clone + 설정파일 자동 생성
6. **설정파일 채우기**:
   - `image-picker/nas.config.json` → NAS 읽기전용 host/user/password (**지현호가 jayce에게 전달 필요**)
   - `studio/studio.config.json` → `"user"`가 본인 이메일인지 확인(3단계 먼저 했으면 자동)
7. **`setup.command` 다시 실행** → `http://localhost:8790` 오픈
8. **[상품 불러오기]** 로 국내/해외 크롤 (최초 1회)
9. 확인: 📚 모범 패널에 "나(jayce@rxc.co.kr) = 등록 가능 ✅" 뜨면 정상

---

## 5. 지금까지 해결한 이슈 (참고용)

| 증상 | 원인 | 해결 |
|---|---|---|
| `EADDRINUSE :::8790` | 이전 서버가 이미 실행 중 | `lsof -ti:8790 \| xargs kill` 후 재실행 |
| push 시 `Invalid username or token` | 만료/오타/권한없는 토큰, 또는 키체인 캐시 | 키체인 초기화 후 올바른 토큰으로 재시도 |
| push 시 `Repository not found` | 원격 저장소 미생성 or 접근권 없음 | GitHub에서 빈 repo 생성 / 접근 가능한 owner로 |
| push 시 `Permission denied (403)` | 토큰에 쓰기 권한 없음(fine-grained Contents 누락 등) | **Classic 토큰 + `repo` scope** 발급이 가장 확실 |
| 커밋에 비밀파일 포함(ai.config.json 등) | `.gitignore` 정비 전 커밋 | `.gitignore` 보강 후 `git rm -r --cached -f . && git add -A && git commit --amend` |
| 서버 시작 시 `Username for github` 프롬프트 | 코퍼스 자동 pull이 git 자격증명 요구 | 각 PC 최초 1회 토큰 인증(osxkeychain 저장) |

### git 인증 팁 (macOS)
- 자격증명은 osxkeychain에 캐시됨. 잘못된 토큰이 캐시됐으면 초기화:
  ```bash
  printf "protocol=https\nhost=github.com\n\n" | git credential-osxkeychain erase
  ```
- 토큰은 **Classic + `repo` scope**가 가장 단순. push 시 Password 칸에 토큰 붙여넣기(**화면에 안 보여도 정상**, Enter).
- ⚠️ **토큰은 Slack·채팅에 절대 붙여넣지 말 것**(유출 시 GitHub Settings→Tokens에서 즉시 폐기).

---

## 6. jayce가 겪을 수 있는 문제 → 대응

- **clone이 안 됨(권한 오류)** → 초대 미수락 상태. 초대 메일 수락 확인.
- **콘텐츠 생성이 안 뜸/멈춤** → `claude login` 안 됨. 터미널에서 로그인.
- **이미지가 안 나옴** → `nas.config.json` 미설정 또는 NAS 계정 오류. host/user/password 확인.
- **"등록 가능"이 아니라 "읽기 전용"으로 뜸** → `git config user.email`이 curators.json 값(`jayce@rxc.co.kr`)과 불일치. 이메일 재설정.
- **모범이 안 보임/최신이 아님** → 코퍼스 clone 경로(`studio.config.json`의 `referenceRepoDir`) 확인, 앱의 [🔄 최신 모범 받기].
- **서버 시작 시 github username 물어봄** → 5절 git 인증 팁 참고(최초 1회 인증).

---

## 7. 남은 작업 (TODO)

- [ ] jayce를 저장소 2곳에 **협업자(Write) 초대** → jayce 수락
- [ ] jayce에게 **NAS 읽기전용 계정** 정보 전달
- [ ] 각 PC git 자격증명 1회 설정(서버 startup 프롬프트 방지)
- [ ] (선택) 코드 커밋 identity 정리(현재 일부 `mark@…local` 자동값)
- [ ] (선택·품질 엄격화) 🧠 "저장 콘텐츠 학습 분석"이 개인 저장 기반이라 사용자별 편차 요인 → 공유 코퍼스로 이전 + 담당자 전용화 검토

---

## 8. 이어서 작업할 때
- 기능·코드 구조 상세: `studio/작업내역_HANDOFF.md`
- 배포·운영·트러블슈팅(이 문서): `배포_핸드오프.md`
- 새 사용자 설치 안내: `README_배포.md`
